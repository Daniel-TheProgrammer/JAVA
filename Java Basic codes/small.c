#include <stdio.h>

int main()
{
    int a, b, sum;
    printf("Enter a numer: ");
    scanf("%d", &a);
    printf("Enter a Second number: ");
    scanf("%d", &b);

    sum = a + b;

    printf("Sum = %d ", sum);
    return 0;
}